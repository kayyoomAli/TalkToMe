import {StyleSheet} from 'react-native';
import {vw} from '../utils/dimensions';

export const routeStyles = StyleSheet.create({
  iconStyle: {height: vw(20), width: vw(20), resizeMode: 'contain'},
});
